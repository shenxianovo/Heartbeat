# ADR-012: Raw Input Event Tracking (Keyboard & Mouse)

## Status: Proposed

## Date: 2026-06-29

(pending implementation)

## Context

We want to track keyboard keystroke and mouse click counts on the desktop, purely as a personal "fun stats" feature (e.g. "how many times did I press space this week", and later "typing speed"). The collected data is uploaded to the self-hosted server and visualized on the dashboard, reusing the existing Collection → Analytics → Dashboard pipeline.

The key tension was **storage shape vs. future analyzability**. Several alternatives were weighed:

1. **Aggregate counts only** (total keystrokes / total clicks per window): cheapest and most privacy-preserving, but cannot ever reconstruct per-key frequency or typing speed.
2. **Time-bucketed per-key counts** (e.g. per-hour `(KeyCode, BucketStart, Count)`): small storage, but buckets **dilute** any speed metric — `keystrokes / 3600s` is meaningless when typing happens in bursts. A larger bucket destroys the "typing speed" use case entirely.
3. **Raw event stream** (one row per key/click with absolute timestamp): largest volume and effectively a keylogger's data structure, but the only shape that supports arbitrary post-hoc analysis (speed curves, per-key frequency, time-of-day heatmaps).

Storage was evaluated and found to be a non-constraint: naive one-row-per-event is ~1 GB/year at 50k events/day on a self-hosted PostgreSQL — negligible. So delta-encoding the timestamps was rejected as premature optimization that would also harm SQL queryability (the thing we most value).

**Privacy note (accepted):** per-key detail + millisecond timestamps uploaded to the server means the server-side store is equivalent to keylogger output. This is a single-user, self-hosted, personal deployment; the owner has accepted this explicitly.

## Decision

Adopt a **raw input event stream**: one row per input event, stored in a single wide table.

### Data model — `InputEvent` (single wide table)

| Column | Type | Notes |
|--------|------|-------|
| `Id` | `uuid` (v7) | Primary key **and** dedup key. Generated by the Agent via `Guid.CreateVersion7()`. |
| `DeviceId` | `bigint` FK | Resolved server-side from `X-Hardware-Id` + JWT OwnerId (not in the upload DTO). |
| `EventType` | `smallint` | `1 = KeyDown`, `2 = MouseButton`, `3 = MouseScroll`. |
| `Code` | `smallint` | Keyboard: raw Windows VK code. MouseButton: `1=left, 2=right, 3=middle`. Scroll: `1=up, 2=down`. |
| `Timestamp` | `timestamptz` | Absolute, millisecond precision. The authoritative time source for speed calculations. |

- **No time bucketing, no delta encoding.** Absolute millisecond timestamp per row.
- **Raw VK codes**, not human-readable names — names are mapped at the presentation layer.

### Idempotency

The Agent generates a **UUIDv7** per event at creation time, used as the row's `Id`. The server inserts with `ON CONFLICT (Id) DO NOTHING`, so retries (ADR-008 offline cache, lost ACKs) are naturally safe. UUIDv7 is time-ordered, giving good append-only write locality on the index. The independent `Timestamp` column is **retained** as the authoritative time source (v7's millisecond ordering is not reliable within the same millisecond, and decoding time from the uuid for every query would be wasteful).

### Collection (Agent)

- A new `InputEventCollector` (`IHostedService`) owns a **dedicated** hook thread running `WH_KEYBOARD_LL` / `WH_MOUSE_LL` with its own message pump, **isolated** from the existing WinEvent thread (low-level hooks are dropped silently by Windows if the callback exceeds `LowLevelHooksTimeout` ~300ms).
- Callback discipline: classify → generate v7 → enqueue → return immediately. No I/O, no contended locks in the callback.
- **KeyDown only.** KeyUp is used only in-memory to track held-key state for **auto-repeat filtering** (holding a key fires repeated KeyDowns); KeyUp is never persisted.
- **Scroll:** one event per notch (±`WHEEL_DELTA`=120); fractional deltas (precision touchpads) are accumulated and normalized to whole notches.
- **Buffer:** `ConcurrentQueue` capped (~100k events), oldest dropped on overflow — a 24/7 tray process must not grow memory unbounded.
- **Offline:** events use the existing ADR-008 LocalCache for offline retry; v7 dedup makes re-upload safe.
- **Upload cadence:** reuses the existing `UsageUploadWorker` interval loop; no new config.

### Contract (Shared Kernel)

- New endpoint **`POST /input-events`** (`[Auth]` + `X-Hardware-Id`, same as other client upload endpoints).
- DTOs live in `Heartbeat.Core`: `InputEventBatchDto { List<InputEventItem> }`, item = `Id, EventType, Code, Timestamp`. `DeviceId` is not in the DTO.

### Reporting (Analytics / Dashboard)

Dedicated-endpoint style (ADR-006). This version ships **only** `GET /input-events/counts`:

- Params: `deviceId?`, `start`, `end` (timezone boundary supplied by the frontend, per Shared Kernel conventions).
- Returns a single total via `GROUP BY EventType, Code`:

```json
{ "keyboardTotal": 48213, "mouseLeft": 6201, "mouseRight": 842,
  "mouseMiddle": 33, "scrollUp": 1502, "scrollDown": 1789 }
```

## Consequences

- ✅ Raw stream supports arbitrary future stats without schema change (just add endpoints).
- ✅ UUIDv7 gives lock-free client-side dedup + good write locality, with no client-maintained sequence state.
- ✅ Single wide table keeps the upload pipeline and queries simple; new input types = new `EventType` value.
- ✅ Reuses existing worker, LocalCache, and auth — minimal new infrastructure.
- ⚠️ Per-key + millisecond data on the server is equivalent to keylogger output (accepted for single-user self-hosted use).
- ⚠️ Higher write volume than AppUsage (~50k rows/day); fine for PostgreSQL at this scale, but not free forever.
- ⚠️ Low-level input hooks are latency-sensitive; a slow callback gets the hook silently unhooked. Callback work must stay minimal.
- ⚠️ Deferred (not yet built): typing-speed endpoint, per-key frequency, time-of-day distribution, mouse movement tracking.

## References

<!-- Filled in as implementation lands -->

- `shared/Heartbeat.Core/DTOs/Input/` — input event DTOs (pending)
- `desktop/Heartbeat.Agent/Services/InputEventCollector.cs` — capture (pending)
- `server/Heartbeat.Server/` — `InputEvent` entity, upload + counts endpoints (pending)
