## Agent skills

### Issue tracker

本地 markdown 模式，issue 以文件形式存放在 `.scratch/` 下。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认标签词汇表。详见 `docs/agents/triage-labels.md`。

### Domain docs

多上下文布局（CONTEXT-MAP.md + 各子目录 CONTEXT.md）。详见 `docs/agents/domain.md`。
